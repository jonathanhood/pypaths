from setuptools import setup

setup(
    name='pypaths',
    version='0.1.2',
    author='Jonathan Hood',
    author_email='me@jonathanhood.org',
    packages=['pypaths'],
    description='Small, but extensible, pathfinding library',
    long_description=open('README.rst').read(),
    install_requires=[]
)

